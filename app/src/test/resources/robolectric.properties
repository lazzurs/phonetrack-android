# Emulate the SDK the app targets (targetSdk in app/build.gradle.kts). The unit tests run on
# JDK 21 through a Gradle toolchain, which Robolectric needs for SDK 35+.
sdk=36
# conscrypt-android on the classpath has no host JNI library
conscryptMode=OFF

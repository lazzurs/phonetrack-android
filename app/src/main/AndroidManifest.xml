<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.eneiluj.nextcloud.phonetrack">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- For auto start -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- For web sync retries -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Needed if app targets API >= 21 -->
    <uses-feature android:name="android.hardware.location.gps" />
    <uses-feature android:name="android.hardware.location.network" />

    <application
        android:name="it.eneiluj.nextcloud.phonetrack.util.PhoneTrack"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.SplashscreenActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.LogjobsListViewActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
            <meta-data
                android:name="android.app.default_searchable"
                android:value="it.eneiluj.nextcloud.phonetrack.android.activity.NotesListViewActivity" />
        </activity>

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.SettingsActivity"
            android:label="@string/settings_server_settings"
            android:parentActivityName="it.eneiluj.nextcloud.phonetrack.android.activity.PreferencesActivity"
            android:windowSoftInputMode="stateVisible" />

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.PreferencesActivity"
            android:label="@string/action_settings"
            android:parentActivityName="it.eneiluj.nextcloud.phonetrack.android.activity.LogjobsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/Preferences"
            />

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.EditLogjobActivity"
            android:label="@string/simple_edit"
            android:parentActivityName="it.eneiluj.nextcloud.phonetrack.android.activity.LogjobsListViewActivity"
            android:windowSoftInputMode="stateHidden"
            android:launchMode="singleTask">
            <!-- this is to receive share action from other apps -->
            <intent-filter android:label="@string/action_create_share">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.AboutActivity"
            android:label="@string/simple_about"
            android:parentActivityName="it.eneiluj.nextcloud.phonetrack.android.activity.LogjobsListViewActivity" />

        <activity
            android:name="it.eneiluj.nextcloud.phonetrack.android.activity.SelectSingleLogjobActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service
            android:name="it.eneiluj.nextcloud.phonetrack.android.quicksettings.NewLogjobTileService"
            android:icon="@drawable/ic_quicksettings_new"
            android:label="@string/action_create"
            android:description="@string/action_create"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name="it.eneiluj.nextcloud.phonetrack.service.LoggerService"
            android:enabled="true"
            android:exported="false" />

    </application>
</manifest>
